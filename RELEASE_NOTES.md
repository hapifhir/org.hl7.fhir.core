## Validator Changes

* Exempt canonical references fixed to latest from pinning checks 
* Use the right version when validating cross-version extensions
* Upgrade to version 0.1.0 for cross-version packages

## Other code changes

* Fix for wrong web URLs in xver packages
* Revise handling of cross-version extension contexts when generating version variant packages
* start working on new package management infrastructure
* Add x-version support to snapshot tests
* Update CanonicalResource test case for change to uniqueness management for canonical resources
* Fix bugs generating snapshots
* fix error in ValueSet version conversion with cross-version extensions
* Answer was not rendering (small case error in the code)
